# Domain Knowledge

This directory contains implementation knowledge about Lethal Company. It does
not define AutoTerminalScanClassic's product model or design decisions.

- [Terminal scan](terminal-scan.md) records the v81 callbacks, item predicate,
  timing gate, and implementation choices used to reason about terminal scans.
